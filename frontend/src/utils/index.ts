// Utility functions for the POE Knowledge Assistant
// This file will contain helper functions

export function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
}
